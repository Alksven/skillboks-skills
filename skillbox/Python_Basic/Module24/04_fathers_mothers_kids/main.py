from classes import Parent
from classes import Child
import random


while True:
    name_parent, age_parent = input("Enter parent's name and age: ")
    add_child = input('{} has a children? '.format(name_parent)).lower()
    if add_child == 'yes':


